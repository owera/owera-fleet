# Binaries
/bin/
/dist/
*.test
*.out
# Compiled fleetctl at the repo root only — must not match cmd/fleetctl/ source.
/fleetctl

# Go
/vendor/
go.work
go.work.sum

# IDE / OS
.DS_Store
.idea/
.vscode/

# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.gpg
/.secrets/

# Local runtime artifacts
/tmp/
/.runtime/

# In-tree git worktrees (specialized-agent sandboxes)
/.worktrees/

# Plan source-of-truth doc lives at /Users/claw3/owera-fleet root but is
# kept out of git so it can be regenerated/edited freely between waves.
/knowing-all-you-now-calm-leaf.md

# Coverage
coverage.out
coverage.html
